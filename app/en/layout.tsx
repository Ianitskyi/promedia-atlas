import type {Metadata} from 'next';
export const metadata:Metadata={title:'Media Atlas — ProMedia',description:'Map and directory of registered media in Ukraine, with filters, analytics and registration guidance.',alternates:{languages:{uk:'https://atlas.promedia.report/',en:'https://atlas.promedia.report/en'}}};
export default function EnglishLayout({children}:{children:React.ReactNode}){return children}
